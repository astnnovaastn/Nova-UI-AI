from setuptools import setup, find_packages

setup(
    name="groq",
    version="0.0.1-mock",
    description="Mock Groq client for Python 3.14 compatibility",
    packages=find_packages(),
    python_requires=">=3.8",
)
