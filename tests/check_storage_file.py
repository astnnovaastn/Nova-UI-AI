import json\n\n# Check the current state of the storage file\nwith open('data/Mem0_storage_ai.json', 'r') as f:\n    data = json.load(f)\n\nprint('Current conversations:', len(data.get('conversation', [])))\nprint('Current memory events:', len(data.get('memory_events', [])))\n\n# Show the last few conversations\nconversations = data.get('conversation', [])\nif conversations:\n    print('\\nLast 3 conversations:')\n    for i, conv in enumerate(conversations[-3:], 1):\n        content = conv.get('content', 'No content')\n        timestamp = conv.get('timestamp', 'No timestamp')\n        print(f'  {i}. [{timestamp[:19]}] {content[:100]}{\"...\" if len(content) > 100 else \"\"}')\n